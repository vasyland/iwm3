export interface Signup {
    id: string;
    access_token: string;
    access_token_expiry: number;
    token_type: string;
    user_name: string;
}

     // refresh_token: string
    // refresh_token_expiry: number